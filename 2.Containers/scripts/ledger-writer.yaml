apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledger-writer
  namespace: bank-of-msa
  labels:
    app: ledger-writer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ledger-writer
  template:
    metadata:
      labels:
        app: ledger-writer
    spec:
      containers:
        - name: ledger-writer
          image: bank-of-anthos/ledger-writer:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          env:
            - name: DB_HOST
              value: "ledger-db"
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: "ledger"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: DB_PASSWORD
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: ledger-writer
  namespace: bank-of-msa
spec:
  selector:
    app: ledger-writer
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP
