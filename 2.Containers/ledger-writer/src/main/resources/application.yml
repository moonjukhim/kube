spring:
  application:
    name: ledger-writer
  datasource:
    url: jdbc:postgresql://${DB_HOST:ledger-db}:${DB_PORT:5432}/${DB_NAME:ledger}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083

